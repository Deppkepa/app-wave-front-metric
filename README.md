# app-wave-front-metric

# Установка

В путь, в котором нет кириллицы:

`git clone https://github.com/Deppkepa/app-wave-front-metric.git`

Из корня проекта:

`pip install -r requirements.txt`

# Запуск

Из корня проекта:

`python main.py`
